#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <cmath>

using std::cin;
using std::cout;
using std::endl;
using std::string;

// int, double, char, string, bool

inline string keep_window_open() {
  string ch;
  cin >> ch;
  return ch;
}
